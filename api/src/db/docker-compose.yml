version: "3.8"
services:
    favo_db:
        environment:
            POSTGRES_USER: postgres
            POSTGRES_PASSWORD: postgres
            POSTGRES_DB: favo
        volumes:
            - postgres_data:/var/lib/postgresql/docker
        ports:
            - 5432:5432
        image: postgres:18
