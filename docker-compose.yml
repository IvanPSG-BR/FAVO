services:
    db:
        environment:
            POSTGRES_USER: postgres
            POSTGRES_PASSWORD: postgres
            POSTGRES_DB: favo
        volumes:
            - ./api/src/db/postgres_data:/var/lib/postgresql/docker
        ports:
            - 5432:5432
        image: postgres:18
